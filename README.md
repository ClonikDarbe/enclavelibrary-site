# Enclave Library Website

Official static website for Enclave Library.

## Structure

- `index.html` main landing page
- `assets/css/style.css` shared design system
- `pages/` static pages
- `auth/confirm.html` Supabase email confirmation page
- `api/version.json` update manifest

Deployed with Cloudflare Pages.
